{
  "buildCommand": "npm install && npx turbo run build --filter=@teezy/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "echo 'install handled in build'",
  "devCommand": "npx turbo run dev --filter=@teezy/web",
  "framework": null
}
